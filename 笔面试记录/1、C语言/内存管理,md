# 内存管理 
## gcc编译的c语言程序占用的内存分为几个部分,初始化的值是多少
1、栈区：存放函数的参数、局部变量
2、堆区：提供程序员动态申请的内存空间
3、全局（静态区）：存放全局变量和静态变量，初始化不为0的全局变量和静态变量
4、代码区：存放函数体的二进制代码和字符串常量

## 存储的大小端
- 小端：数据的高位字节存储在低地址
- 大端：数据的高位字节存储在高地址

## 全局变量和局部变量的区别
1、全局变量存放在静态区，进入main函数之前被创建，生命周期为整个源程序
2、局部变量在栈中分配，在函数被调用的时候才被创建，函数退出时销毁，生命周期在函数内

## C语言中的memcpy 和 memmove 是一样的吗
> 两者功能类似，用来拷贝 src 所指向内存内容前n个字节到dest所指向的地址上
> 不同的是，当 src 和 dest 所指的内存区域重叠时，memcpy 可能无法正确处理，

## malloc 底层是如何实现的
1、malloc函数的底层实现是os 有一个可用内存块连接成的空闲链表，调用malloc时，将遍历链表寻找足够大的内存空间，调用free的时候，内存块会重新返回链表
2、若内存块过于琐碎无法满足用户需求，则os会合并相邻的内存块

## 1G内存的计算机中能否通过malloc申请超过1G的内存
> 可以，虚拟内存和物理内存没有直接关系，映射由OS完成

