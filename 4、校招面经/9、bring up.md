1、芯片的bring up 主要有哪些工作
    sdk 编译、烧录、启动、调试串口

2、static 修饰局部变量 全局变量 函数的作用

    局部变量 : 改变变量的存储位置（由栈转为静态存储区段）和生命周期（由函数调用期间转为整个程序运行期间，但是不改变作用域
    全局变量 ： 限制了全局变量的作用域，使其在其他定义域中不可见，不可以被extern 引用
    函数 ： 限制函数的作用域，使其仅可在定义它的源文件内可见，其他文件无法调用

    