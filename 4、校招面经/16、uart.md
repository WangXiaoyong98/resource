1、UART 协议与其基本特点
    1、uart 通用异步收发器 串行通信协议
    2、异步通信，不需要时钟线
    3、全双工通信，可以同时发送和接受
    
2、信号线
    1、tx
    2、rx
    3、流控线 RTS 表示本设备准备好接收数据
             CTS 表示对方设备准备好接收数据

3、uart 得标准帧格式主要包括

起始位：1位 固定为低电平，表示帧开始
数据位：5-9位（通常为 8 位），LSB 先发送
校验位 ： 0-1 位 用于错误检测
停止位：1-2位 固定为高电平，表示帧结束

